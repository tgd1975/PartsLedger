---
id: TASK-005
title: Author .claude/settings.json with full allowlist + deny
status: open
opened: 2026-05-12
effort: Small
complexity: Junior
human-in-loop: No
epic: align-with-circuitsmith
order: 5
prerequisites: [TASK-004]
---

## Description

PartsLedger currently has only a 3-line `.claude/settings.local.json`
with one project-specific permission. Author a proper checked-in
`.claude/settings.json` mirroring CircuitSmith's allowlist + deny
shape.

**Allowlist** (adapted to PartsLedger's script names):

```text
Bash(python scripts/housekeep.py:*)
Bash(python scripts/release_burnup.py:*)
Bash(python scripts/release_snapshot.py:*)
Bash(python scripts/task_system_config.py:*)
Bash(python scripts/update_idea_overview.py:*)
Bash(python scripts/update_task_overview.py:*)
Bash(python scripts/update_scripts_readme.py:*)

Bash(uv venv:*)
Bash(uv pip install:*)
Bash(uv pip sync:*)
Bash(uv run:*)
Bash(uv sync:*)

Bash(.venv/bin/python:*)
Bash(.venv/bin/pytest:*)
Bash(.venv/bin/ruff:*)

Bash(git status:*)
Bash(git diff:*)
Bash(git log:*)
Bash(git show:*)
Bash(git branch:*)
Bash(git rev-parse:*)
Bash(git ls-files:*)
Bash(git ls-remote:*)
Bash(git blame:*)

Bash(ls:*)
Bash(find:*)
Bash(grep:*)
Bash(rg:*)
Bash(wc:*)

Bash(markdownlint-cli2:*)
Bash(ruff:*)

Bash(pytest:*)
Bash(python -m pytest:*)

Bash(gh pr list:*)
Bash(gh pr view:*)
Bash(gh issue list:*)
Bash(gh issue view:*)
Bash(gh api repos/:*)
```

**Deny** (verbatim from CircuitSmith):

```text
Bash(git add:*)

Bash(sed:*)
Bash(awk:*)
Bash(head:*)
Bash(tail:*)

Bash(git push origin main:*)
Bash(git push --force:*)
Bash(git push -f:*)
```

No `PreToolUse` hook block in this task — the `codeowner_hook.py`
PreToolUse hook lands in TASK-009.

**Drop** from CircuitSmith's allowlist:

- `Bash(python scripts/security_review_changes.py:*)` — added later
  in TASK-008 with the security-review hooks.
- `Bash(python scripts/portability_lint.py:*)` — camel-vs-giraffe.
- `Bash(python scripts/generate-schematic.py:*)` — camel-vs-giraffe.

(Both arrive in TASK-005's allowlist as comments in their final
position so the diff in TASK-008 is small.)

## Acceptance Criteria

- [ ] `.claude/settings.json` exists at repo root and validates as
      JSON.
- [ ] All allowlist entries listed above are present.
- [ ] All deny entries listed above are present.
- [ ] `Bash(git add:*)` is in `deny` (so the new `/commit` skill's
      no-`git add` contract is harness-enforced).
- [ ] PartsLedger's existing `.claude/settings.local.json` remains
      unchanged (it covers per-developer overrides; not in this
      task's scope).

## Test Plan

1. `python -c "import json; json.load(open('.claude/settings.json'))"`
      exits 0.
2. After the file lands, attempt a `Bash(git add foo)` tool call from
      a fresh Claude session — should be denied by the harness
      without prompting.

## Notes

`.claude/security-review-latest.md` is `.gitignore`d in TASK-002.
That file is generated by the security-review hooks landing in
TASK-008; it has no bearing on `.claude/settings.json`.
